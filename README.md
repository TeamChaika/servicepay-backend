# ServicePay Backend

FastAPI бэкенд для платформы ServicePay - SaaS для кафе и ресторанов.

## 🎯 Возможности

- 🎫 **Продажа билетов** на мероприятия с различными типами
- 💰 **Прием депозитов** для резервирования столиков
- 📱 **Оплата через СБП** (QR Manager интеграция)
- 👥 **Управление персоналом** с ролями и правами
- 📊 **Аналитика и отчеты** по продажам и мероприятиям
- 💳 **Внутренний биллинг** с комиссиями и подписками
- 📧 **Email/SMS уведомления** для пользователей
- ⭐ **Система отзывов** о мероприятиях
- 🗺️ **Геолокация** мероприятий
- 🔐 **Двухфакторная аутентификация** (2FA)

## 🛠 Технологии

- **FastAPI** - современный async фреймворк для Python
- **PostgreSQL 16** - надежная реляционная БД
- **Redis** - кеширование и очереди задач
- **SQLAlchemy 2.0** - ORM для работы с БД
- **Alembic** - миграции базы данных
- **Celery** - фоновые и периодические задачи
- **Pydantic v2** - валидация данных
- **JWT** - безопасная аутентификация

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Git

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd servicepay-backend
```

2. **Создайте .env файл**
```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите:
- `SECRET_KEY` - случайная строка для JWT
- `ENCRYPTION_KEY` - 32-байтный ключ для шифрования (base64)
- `JWT_SECRET_KEY` - ключ для JWT токенов
- Настройки SMS и Email провайдеров
- `DATABASE_URL` - строка подключения к PostgreSQL
- `REDIS_URL` - строка подключения к Redis

3. **Запустите проект**
```bash
make up
# или
docker-compose up -d
```

4. **Примените миграции БД**
```bash
make migrate
# или
docker-compose exec backend alembic upgrade head
```

5. **Откройте API документацию:**
- 🔧 API документация: http://localhost:8000/docs

## 📝 Команды Makefile

```bash
make help        # Показать все команды
make build       # Собрать контейнеры
make up          # Запустить backend
make down        # Остановить backend
make logs        # Посмотреть логи
make migrate     # Применить миграции
make shell       # Войти в backend shell
make test        # Запустить тесты
make clean       # Очистить все данные
```

## 📦 Структура проекта

```
/
├── app/
│   ├── api/            # API endpoints
│   ├── models/         # SQLAlchemy модели
│   ├── schemas/        # Pydantic схемы
│   ├── services/       # Бизнес-логика
│   ├── core/           # Безопасность, шифрование
│   ├── tasks/          # Celery задачи
│   └── websocket/      # WebSocket handlers
├── alembic/            # Миграции БД
└── tests/              # Тесты
```

## 🔧 Разработка

### Локальная разработка

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите PostgreSQL и Redis через Docker
docker-compose up -d postgres redis

# Запустите backend локально
uvicorn app.main:app --reload
```

### С Docker

```bash
# Запустить все сервисы (PostgreSQL, Redis, Backend, Celery)
make up

# Посмотреть логи
make logs

# Применить миграции
make migrate
```

## 🗄 База данных

### Создание миграции

```bash
docker-compose exec backend alembic revision --autogenerate -m "Description"
```

### Применение миграций

```bash
make migrate
# или
docker-compose exec backend alembic upgrade head
```

### Откат миграции

```bash
docker-compose exec backend alembic downgrade -1
```

## 📊 Основные модели данных

- **User** - пользователи (владельцы, персонал, гости)
- **Venue** - заведения (кафе, рестораны)
- **Event** - мероприятия
- **Payment** - платежи (депозиты, билеты, пополнение)
- **Ticket** - билеты на мероприятия
- **TicketType** - типы билетов (VIP, Standard, etc.)
- **Balance** - балансы пользователей
- **BalanceTransaction** - транзакции по балансу
- **Terminal** - терминалы для приема платежей
- **Refund** - возвраты средств
- **Review** - отзывы о мероприятиях
- **Staff** - персонал заведений

## 🔐 Безопасность

- JWT токены для аутентификации
- Двухфакторная аутентификация (TOTP)
- Шифрование чувствительных данных (API ключи)
- HTTPS для production
- CORS политика
- Rate limiting (рекомендуется добавить)

## 📱 API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - регистрация
- `POST /api/v1/auth/login` - вход
- `GET /api/v1/auth/me` - текущий пользователь
- `POST /api/v1/auth/2fa/setup` - настройка 2FA

### Заведения
- `GET /api/v1/venues` - список заведений
- `POST /api/v1/venues` - создать заведение
- `PUT /api/v1/venues/{id}` - обновить
- `DELETE /api/v1/venues/{id}` - удалить

### Мероприятия
- `GET /api/v1/events` - список мероприятий
- `POST /api/v1/events` - создать мероприятие
- `GET /api/v1/events/{id}` - детали мероприятия
- `POST /api/v1/events/{id}/ticket-types` - добавить тип билета

### Платежи
- `POST /api/v1/payments` - создать платеж
- `GET /api/v1/payments/{id}` - статус платежа
- `POST /api/v1/deposits` - внести депозит

### Билеты
- `POST /api/v1/tickets/purchase` - купить билет
- `POST /api/v1/tickets/verify` - проверить билет
- `POST /api/v1/tickets/{id}/use` - использовать билет

### Баланс
- `GET /api/v1/balance` - текущий баланс
- `POST /api/v1/balance/topup` - пополнить
- `GET /api/v1/balance/transactions` - история

### Аналитика
- `GET /api/v1/analytics/dashboard` - дашборд
- `GET /api/v1/analytics/revenue` - доходы

Полная документация: http://localhost:8000/docs

## 🔄 Celery задачи

### Периодические задачи
- Проверка статуса платежей (каждую минуту)
- Списание месячной подписки (раз в день)
- Проверка низких балансов (каждый час)

### Фоновые задачи
- Отправка уведомлений
- Обработка комиссий
- Генерация QR кодов

## 🧪 Тестирование

```bash
# Запустить тесты
make test
# или
docker-compose exec backend pytest

# С покрытием
docker-compose exec backend pytest --cov=app tests/
```

## 📈 Мониторинг

### Логи

```bash
# Все сервисы
make logs

# Только backend
make backend-logs

# Только Celery
make celery-logs
```

### Здоровье сервисов

- Backend health: http://localhost:8000/health
- PostgreSQL: `docker-compose exec postgres pg_isready`
- Redis: `docker-compose exec redis redis-cli ping`

## 🚀 Production деплой

### Подготовка

1. Измените `DEBUG=False` в `.env`
2. Укажите production домены в `CORS_ORIGINS`
3. Используйте безопасные пароли для БД
4. Настройте backup для PostgreSQL
5. Настройте SSL сертификаты

### Запуск

```bash
docker-compose up -d
```

## 📞 Интеграции

### СБП (QR Manager)
Интеграция с QR Manager для приема платежей через СБП.
Настройте API ключи в `app/services/qr_manager_client.py`

### SMS (SmsAero)
Отправка SMS уведомлений через SmsAero.
Укажите `SMSAERO_EMAIL` и `SMSAERO_API_KEY` в `.env`

### Email (SendGrid / SMTP)
Отправка email через SendGrid или SMTP.
Настройте в `.env`

## 🔗 Зависимости от других сервисов

Этот backend работает независимо, но может быть интегрирован с:
- **Frontend Admin** - админская панель (Vue 3)
- **Frontend Guest** - гостевой портал (Vue 3)

Настройте CORS в `.env` для разрешения запросов с фронтендов.

## 🤝 Участие в разработке

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Proprietary

## 👨‍💻 Автор

HelpChaika Team

---

**Made with ❤️ by HelpChaika Team**
